---
title: "API Reference"
description: "Automated gRPC API reference generated from proto definitions"
---

# API Reference

The gRPC API is defined in the [proto repository](https://github.com/nantian-gw/proto).
Run `buf generate` to regenerate the API reference.

## Generating Docs

```bash
cd proto && buf generate
```

The generated API reference is available at `/api/`.
