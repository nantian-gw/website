---
title: Wasm 插件
description: "使用实验性 WasmPlugin API 把沙箱化 Wasm 模块加载到 Nantian Gateway 1.5 数据面。"
slug: zh/1.5/features/wasm-plugins
---

Wasm 插件让平台团队不用重新构建数据面，就能添加自定义 request、response 或 stream 处理逻辑。插件通过 `WasmPlugin` 资源声明，并在 Rust 数据面运行时中执行。

:::caution[实验功能]
`WasmPlugin` 使用 `gateway.nantian.dev/v1alpha1`。生产发布前，先安装 CRD、启用实验功能，并完整测试每条插件路径。
:::

## 插件模型

| 部分 | 字段 | 作用 |
| --- | --- | --- |
| 模块来源 | `wasm.url`, `wasm.configMap`, `wasm.inline`, `wasm.sha256` | 定位并校验 `.wasm` 模块。 |
| 目标绑定 | `targetRefs` | 把插件绑定到本地 Gateway API 目标资源。 |
| Hook 选择 | `hooks` | 选择 `onRequest`、`onResponse` 或 `onStreamChunk`。 |
| 沙箱限制 | `sandbox` | 限制内存、执行时间、网络和文件系统访问。 |

## 示例

```yaml
apiVersion: gateway.nantian.dev/v1alpha1
kind: WasmPlugin
metadata:
  name: request-audit
  namespace: nantian-demo
spec:
  wasm:
    url: https://example.com/plugins/request-audit.wasm
    sha256: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: echo
  hooks:
    - onRequest
    - onResponse
    - onStreamChunk
  config: |
    {"mode":"audit"}
  sandbox:
    maxMemoryBytes: 67108864
    maxExecutionTimeMs: 100
    allowNetwork: false
    allowFileSystem: false
```

## 发布检查清单

- 启用实验功能并验证 `wasmplugins.gateway.nantian.dev`。
- 外部模块使用 `sha256` 固定。
- 先使用较小的 `targetRefs` 范围。
- 除非确实需要，否则保持 `allowNetwork` 和 `allowFileSystem` 关闭。
- 观察数据面日志和 metrics 中的插件加载失败、拒绝和延迟。

## 相关页面

- [实验功能](/zh/1.5/configuration/experimental-features/)
- [自定义和实验资源](/zh/1.5/api-reference/crds/)
- [数据面架构](/zh/1.5/architecture/dataplane/)
- [安全与可观测性](/zh/1.5/features/security-observability/)
